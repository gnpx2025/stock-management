# Docker notes

Local PostgreSQL is defined in the repository root `docker-compose.yml` (image `postgres:16-alpine`).

Copy `.env.example` to `.env` before `docker compose up -d`.

Do not commit real secrets.
